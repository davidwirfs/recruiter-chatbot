# Recruiter chatbot — public deployment (HF Spaces).

# Vector store
chromadb>=0.5.0

# Chunking
langchain-text-splitters>=0.3.0

# Embeddings (local model, cached in the Docker image at build time)
sentence-transformers>=3.0.0

# PDF parsing (kept for backward compatibility; current sources are markdown)
pypdf>=4.0.0

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Public-deploy hardening
slowapi>=0.1.9

# Standard helpers
pyyaml>=6.0
