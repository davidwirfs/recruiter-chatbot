# Recruiter chatbot — Phase 1 (local, zero API cost)
# Schema-aligned with _ecosystem/rag/requirements.txt where it overlaps.

# Vector store
chromadb>=0.5.0

# Chunking
langchain-text-splitters>=0.3.0

# Embeddings (local, no API key)
sentence-transformers>=3.0.0

# PDF parsing
pypdf>=4.0.0

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Standard helpers
pyyaml>=6.0
